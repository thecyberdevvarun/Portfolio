import Admin from "@/views/Admin";

export const metadata = {
  title: "Admin",
  description: "Frontend-only authoring tools for local drafts and export.",
};

export default function AdminPage() {
  return <Admin />;
}

